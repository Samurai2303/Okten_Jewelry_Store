let baseURL = 'http://localhost/api';

let urls = {
    auth: {
        login: '/auth',
        refresh: '/auth/refresh'
    },
    users: '/users',
    products: '/products',
    orders: '/orders'
};

export {baseURL, urls};