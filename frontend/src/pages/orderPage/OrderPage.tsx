import {FC} from "react";
import {IOrder} from "../../interfaces";
import css from './OrderPage.module.css';

interface IProps{
    forAdmin: boolean;
}

let OrderPage:FC<IProps> = ({forAdmin}) => {

    return (
        <div>

        </div>
    );
}

export {OrderPage};
